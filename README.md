# golang-multiple-image-upload-multipartformdata
Upload many images using multipart formdata

# Build

`go build`

Then open localhost:9090 in browser.

# Docker

use `docker-compose build` then `docker-compose up`
